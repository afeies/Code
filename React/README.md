- open index.html

- for this vanilla implementation of a webpage without React, you must manually read DOM, update DOM, and keep state in sync.
- the UI logic is spread across files
- harder to reason about when more states appear